# Local tooling and dependencies
/node_modules/
# Secrets
.env
.env.*
!.env.example
# Operating system files
.DS_Store
Thumbs.db
# Runtime bot state created on the bot host (never wipe on update)
/data/*
!/data/README.md
!/data/site-updates.json
!/data/manual-identities.json
# Panel / egg junk that dirties the tree and breaks `git pull`
/downloads/
/tmp/
